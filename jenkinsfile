pipeline {
    agent {
        docker {
            image 'node:18'
        }
    }

    stages {
        stage('Check Node Version') {
            steps {
                sh 'node --version'
            }
        }
    }
}
